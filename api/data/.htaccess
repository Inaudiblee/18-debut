# Block direct public access to the stored JSON files.
# store.php still reads/writes them from the server side normally.

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
